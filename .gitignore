# Xcode build output
build/
DerivedData/
*.xcarchive
*.ipa

# Xcode user state (không share giữa dev)
xcuserdata/
*.xcuserstate
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/

# Swift Package Manager resolved (CI tự resolve)
SourcePackages/
.build/
*.resolved

# Xcode project — được GENERATE bởi xcodegen từ project.yml
# Uncommit nếu bạn muốn mở thẳng từ Finder mà không cần chạy xcodegen
# RPGPlayer.xcodeproj/

# macOS
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db

# Vim/IDE
*.swp
*.swo
.idea/
.vscode/

# Temporary
/tmp/

# mruby + CI-generated Frameworks — produced by Scripts/build_mruby.sh
# Rebuilt on every CI run from source; do NOT commit to repo.
Frameworks/
